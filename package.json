{
  "name": "civic-tech-montreal-toc-canvas",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "test": "vitest run",
    "check": "npm run test && npm run build",
    "deploy": "npm run build && wrangler deploy",
    "deploy:check": "npm run build && wrangler deploy --dry-run"
  },
  "dependencies": {
    "nanostores": ">=1.0.1 <2"
  },
  "devDependencies": {
    "typescript": ">=5.9.2 <6",
    "vite": ">=7.1.5 <8",
    "vite-plugin-singlefile": ">=2.3.0 <3",
    "vitest": ">=3.2.4 <4",
    "wrangler": "^4.129.1"
  }
}
